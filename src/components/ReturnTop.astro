---
export interface Props {
  series: string | null
  title: string
  top: string | null
}

const { series, title, top } = Astro.props
---

{
  series ? (
    <div class="return-top">
      <a href={top}>作品ＴＯＰ</a>
    </div>
  ) : (
    <div class="return-top">
      <a href="/">ＩＮＤＥＸ</a>&nbsp;＞&nbsp;{title}
    </div>
  )
}

<style>
  .page-nav {
    margin: 3px 0;
  }
</style>
